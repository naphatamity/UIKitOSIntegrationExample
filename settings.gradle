pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        maven { url 'https://jitpack.io'}
        google()
        mavenCentral()
        maven {
            url "https://repo.maven.apache.org/maven2/"
        }
    }
}
rootProject.name = "My Application"
include ':app'
include ':social'
include ':amity-uikit'
include ':chat'
include ':common'

project(':social').projectDir = new File(settingsDir, './social')
project(':chat').projectDir = new File(settingsDir, './chat')
project(':common').projectDir = new File(settingsDir, './common')
project(':amity-uikit').projectDir = new File(settingsDir, './amity-uikit')
